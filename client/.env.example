# Vite — production (Vercel)
# Point to your deployed API (include /api path if using full URL)
# VITE_API_BASE_URL=https://your-api.onrender.com/api

# Development: leave unset to use Vite proxy (/api → localhost:5001)
